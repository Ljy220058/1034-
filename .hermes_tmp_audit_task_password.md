# Security audit remediation: password hashing hardening

Context: strengthen password storage in `backend/routes/auth.py`.
Acceptance criteria:
- Replace single SHA-256 hashing with a salted slow hash (e.g. Argon2/bcrypt/passlib).
- Login must verify passwords with constant-time/appropriate verifier.
- Existing auth tests should pass; add coverage for password verification behavior if needed.
- Keep the change scoped to auth and any necessary dependency/config updates.
