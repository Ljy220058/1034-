backend-dev task note:
- Added `/root/autodl-tmp/projects/hermes-swarm-lab/backend/routes/achievements.py` with pure-function achievement rule generation.
- Exposed `GET /api/v1/achievements/sample` returning ApiResponse-shaped payload (`data` + `message`).
- No tests were added because the task explicitly requested no testing; pending pytest verification before completion.
